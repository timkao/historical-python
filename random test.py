#import random
array = []
rs = 20

for i in range(0, 10):
	#random.seed(rs + i)
	#element = random.randrange(0, 10000)
	#array.append(element)

#print array
#random.shuffle(array)
#original = random.choice(array)
#discount = random.random()

#print "%d, %.2f, %.2f" % (original, discount, original*discount)
